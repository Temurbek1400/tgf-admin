import { useRequest } from "hooks/useRequest/useRequest";
import { IUseComments } from "./CommentsContext.type";

const useComments = (): IUseComments => {
  return {
    state: {},
    actions: {},
  };
};

export default useComments;
